import './Title_section.css'

function Title_section(props){

    return (
        <div id="titles">
            <h2 id="title_sections">{props.title}</h2>
        </div>
    ) 
    
}

export default Title_section;